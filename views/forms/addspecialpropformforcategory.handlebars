<form id="addSpecialPropForCategory" class='col-md-3' action="/admin/addspecialpropforcategory" method="post" enctype="multipart/form-data" style="margin-left: 5%;">
    <div class="form-group">
		<div class="input-group">
			<input type="text" class="form-control" id="inputDescribe" name='describe' placeholder="Описание" required>
		</div>
	</div>
	<div class="form-group">
		<select name='category' id='addPropForProduct' class='form-control'>
		<option class='categoryAdd' value='0'>Выберите категорию</option>
		{{#each category}}
		<option class='propAddCategory' value={{_id}}>{{name}}</option>
		{{/each}}
		</select>
	</div>
	 <div class="form-group">
		<div class="input-group">
			<input type="date" class="form-control" name='toDate' placeholder="Дата (гггг-мм-чч)" required>
		</div>
	</div>
	 <div class="form-group">
		<div class="input-group">
			<input type="number" class="form-control" name='discount' min='0' placeholder="Скидка">
		</div>
	</div>
	 <div class="form-group">
		<div class="input-group">
			<input type="number" class="form-control" name='new_price' min='0' placeholder="Новая цена">
		</div>
	</div>
    <div class="form-group">
		<input class="btn btn-primary" type="submit" value="Добавить"></input>
		<a href='#' class='btn btn-default' onclick='$("#addSpecialPropFormPlace").empty().toggleClass("hidden"); return false;'>отмена</a>
 </div>
</form>