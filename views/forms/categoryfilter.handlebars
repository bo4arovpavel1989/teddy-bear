<form id='categoryFilterForm' class='form-inline'>
{{#each category}}
	<input type="checkbox" name="categoryFilter" value='{{_id}}' checked>
	<label>{{name}}</label>
{{/each}}	
 <div class="form-group">
	<input class="btn btn-default" type="submit" value="Применить фильтр"></input>
</div>
</form>